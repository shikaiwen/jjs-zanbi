<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jjs.zanbi.dao.WorkerMapper" >


<!--    <select id="selectByPage" parameterType="com.jjs.zanbi.model.WorkerExample" resultMap="BaseResultMap">

        select


        <include  refid="Base_Column_List" />


        from worker a

        left join

        org
        <if test="_parameter != null">
            <include refid="Example_Where_Clause" />
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>-->

    <select id="selectEx" resultType="map">
        SELECT

        a.*,


        <include refid="com.jjs.zanbi.dao.OrgMapper">
            <property name="alias" value="a"></property>
            <property name="prefix" value="a"></property>
        </include>


        from worker a
        left join
    </select>

<sql id="base_ex">

${alias}.id   		 ${prefix}_id,
${alias}.worker_id   		 ${prefix}_worker_id,
${alias}.name   		 ${prefix}_name,
${alias}.zb_role_id   		 ${prefix}_zb_role_id,
${alias}.org_id   		 ${prefix}_org_id,
${alias}.status   		 ${prefix}_status
</sql>
</mapper>